# Core HTTP / scraping
requests>=2.31
curl_cffi>=0.6          # TLS fingerprint spoofing para bancas com 403
httpx>=0.27
beautifulsoup4>=4.12
lxml>=5.0

# Browser automation (para Cloudflare / JS-heavy)
playwright>=1.44
# Após instalar: playwright install chromium

# PDF
pymupdf>=1.24           # fitz — extração de texto e metadata
pdfplumber>=0.11        # tabelas e layout
pypdf>=4.0              # fallback leve

# Data
pandas>=2.1
pyyaml>=6.0
rapidfuzz>=3.6          # fuzzy matching para deduplicação

# Validation / models
pydantic>=2.5

# TLS chain repair (AIA) for live V2 fetch -- both already present transitively
# (google-auth, requests) but now imported directly by live_abc_adapter.py.
cryptography>=42.0
certifi>=2024.2.2

# Utilities
tqdm>=4.66
python-dotenv>=1.0
pytest>=8.0

# Optional: OCR (instalar só se necessário)
# pytesseract>=0.3
# pdf2image>=1.17
# pillow>=10.0
